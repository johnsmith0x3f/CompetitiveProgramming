#include <bits/stdc++.h>
using namespace std;

inline void solve() {
	int n, m, k, w;
	cin >> n >> m >> k >> w;

}

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	int t;
	cin >> t;
	while(t--) solve();

	return 0;
}
